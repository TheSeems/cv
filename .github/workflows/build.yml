name: Build and Deploy CV

on:
  push:
    branches:
      - main
      - master
    paths-ignore:
      - 'output/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
      
      - name: Setup TinyTeX
        uses: r-lib/actions/setup-tinytex@v2
      
      - name: Install required LaTeX packages
        run: |
          tlmgr install curve fontawesome5 biblatex biblatex-ieee biber xcolor pgf cochineal cabin inconsolata comment csquotes relsize xpatch silence tools carlisle babel-english xstring realscripts || true
      
      - name: Compile CV
        working-directory: src
        run: |
          xelatex -interaction=nonstopmode cv-llt.tex
          biber cv-llt
          xelatex -interaction=nonstopmode cv-llt.tex
          xelatex -interaction=nonstopmode cv-llt.tex
      
      - name: Prepare output directory
        run: |
          mkdir -p output
          mv src/cv-llt.pdf output/cv.pdf
          rm -f src/*.aux src/*.bbl src/*.bcf src/*.blg src/*.log src/*.out src/*.run.xml src/*.toc
      
      - name: Commit PDF to repository
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add output/cv.pdf
          if ! git diff --staged --quiet; then
            git commit -m "Update CV PDF [skip ci]"
            git push
          else
            echo "No changes to commit"
          fi

